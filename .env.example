# URL do PocketBase (backend de obras e artistas)
# Deixe em branco para usar o servidor padrão do TokenNation
NEXT_PUBLIC_PB_URL=https://pocketbase-tokennation.dokploy.tekne.studio

# URL do Google Apps Script (usado pela rota /api/obras)
# Gere em: script.google.com → Implantar como Web App → copie a URL
APPS_SCRIPT_URL=https://script.google.com/macros/s/SEU_ID_AQUI/exec
